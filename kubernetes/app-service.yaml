apiVersion: v1
kind: Service

metadata:
  name: nammaestock-service
  namespace: production

spec:
  type: NodePort

  selector:
    app: nammaestock

  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
